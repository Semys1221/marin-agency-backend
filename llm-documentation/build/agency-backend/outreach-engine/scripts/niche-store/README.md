# niche-store

CRUD operations for `niche_hunts` and `niche_variable` tables in Supabase.

## Usage

```bash
python niche_store.py --tenant marin        # get active hunt
python niche_store.py --tenant marin --demo  # print demo niches
```
